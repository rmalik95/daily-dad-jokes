# Environment Variables Template
# Copy this file to .env.local for local development
# Never commit .env files to version control

# Public API URL (if needed in the future)
# VITE_API_URL=https://api.example.com

# Public configuration (safe to expose to client)
# VITE_APP_NAME=Daily Dad Jokes
# VITE_APP_VERSION=1.0.0

# Note: Only variables prefixed with VITE_ are exposed to the client-side code
# Never add secrets, API keys, or sensitive data here
